export type TNewsItem = {
    id: string
    title: string
    description?: string
    url: string
    imgSrc: string
    imgSrcSet?: string
    downloadedAt?: string
    tag: string
    imageId?: string
}